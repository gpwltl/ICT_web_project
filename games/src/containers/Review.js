import React from 'react';
import BigText from '../components/BigText';

const Review = () => {
    return (
        <div>
            <BigText>리뷰</BigText>
        </div>
    );
};

export default Review;